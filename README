You will need to log into vcloud before you can use this script. It appears that, as long as you run this frequently, there is no need to keep logging in.

For this script to work, you will also need to install the vcloud-walk gem. I used rbenv for this, but you could also install it system-wide
